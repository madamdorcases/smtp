# Pure SMTP relay — trimmed dependencies.
# No FastAPI, no admin panel, no API server. Just SMTP + Redis + DKIM.

# --- Web framework (NONE) ---
# (no fastapi, no uvicorn, no jinja2, no python-multipart, no itsdangerous)

# --- Async / data ---
redis[hiredis]==5.2.1
orjson==3.10.12

# --- Env file loader ---
python-dotenv==1.0.1

# --- SMTP ---
aiosmtpd==1.4.6
aiosmtplib==3.0.2
dkimpy==1.1.8

# --- Crypto / auth ---
cryptography==44.0.0
argon2-cffi==23.1.0

# --- Networking / DNS ---
httpx==0.28.1
dnspython==2.7.0

# --- Logging ---
structlog==24.4.0
tenacity==9.0.0
